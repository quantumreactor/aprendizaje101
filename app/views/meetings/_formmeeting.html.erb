<%= simple_form_for [ @lead, @meeting ] do |m| %>
  <%= m.input :summary %>
  <%= m.input :content, as: :text, input_html: {rows: 10, cols: 40} %>
  <%= m.submit "Subir conversacion", class: "btn btn-primary" %>
<% end %>
